# Docker environment variables
# this is a demo .env file. Copy this to .env and edit the values before running docker-compose up
MYSQL_ROOT_PASSWORD = rootpassword
MYSQL_DATABASE = abet_tools
MYSQL_USER = abet_user
MYSQL_PASS = abet_password